<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.min.dv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ViewEventActivity" />
        <activity android:name=".activity.CanvasActivity" />
        <activity android:name=".activity.RulerActivity" />
        <activity android:name=".activity.MultiTouchActivity" />
        <activity android:name=".activity.ImageViewScaleActivity" />
        <activity android:name=".activity.ViewAnimActivity"></activity>
    </application>

</manifest>
